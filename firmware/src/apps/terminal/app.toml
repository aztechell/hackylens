schema = 1
id = "terminal"
name = "TERMINAL"
version = "0.1.0"
entry = "terminal_enter"
generated_symbol = "hk_generated_app_terminal"
lifecycle = "legacy"
sources = ["terminal_app.c", "terminal_buffer.c", "terminal_controller.c", "terminal_view.c"]
private_includes = []
services = []

[menu]
visible = true
order = 1

[autostart]
eligible = true
id = 1

[capabilities]
required = [
  { id = "hackylens.cap.display", instance = 0, minimum = "0.1.0", maximum_exclusive = "0.2.0", features = ["base-plane", "dirty-regions", "rgb565", "borrowed-surface"] },
  { id = "hackylens.cap.input", instance = 0, minimum = "0.1.0", maximum_exclusive = "0.2.0", features = ["state", "events", "debounced-buttons"] },
]
optional = []

[limits]
static_ram_bytes = 1048576
stack_bytes = 16384
state_bytes = 262144
tick_interval_us = 10000
tick_budget_us = 1000
render_budget_us = 10000

[metadata]
help = "TERMINAL legacy feature app."
debug = "No app-specific debug commands."

[tests]
host_sources = ["terminal_app.c"]
build_profiles = ["full", "disabled"]
